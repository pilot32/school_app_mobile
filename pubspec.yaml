name: school_app_mvp
description: MVP of school app
publish_to: 'none'

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5
  http: ^1.2.2
  shared_preferences: ^2.1.0
  flutter_svg: ^1.1.0
  syncfusion_flutter_pdfviewer: ^31.1.21
  google_fonts: ^6.2.1

flutter_native_splash:
  color: "#F7F7FB"         # Background color (your scaffoldBg maybe)
  image: assets/logo.png   # Path to your logo
  android: true
  ios: true
  web: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_native_splash:
flutter:
  uses-material-design: true

