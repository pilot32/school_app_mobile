import 'package:flutter/material.dart';

class AppColors {
  // Primary blues
  static const Color primary = Color(0xFF7FB3FF);        // Header, bottom nav
  static const Color primaryDark = Color(0xFF1F3A5F);    // Text, active icons

  // Secondary / accents
  static const Color lavender = Color(0xFFF3EFFF);       // Soft backgrounds
  static const Color accentPurple = Color(0xFF8B7CF6);   // Highlights

  // Backgrounds
  static const Color scaffold = Color(0xFFFAF8FF);       // App background
  static const Color card = Color(0xFFFFFFFF);           // Cards

  // Text colors
  static const Color textPrimary = Color(0xFF1F3A5F);
  static const Color textSecondary = Color(0xFF6B7280);

  // Utility
  static const Color divider = Color(0xFFE4E7EC);
  static const Color success = Color(0xFF22C55E);        // Attendance %
  static const Color warning = Color(0xFFF59E0B);
}
